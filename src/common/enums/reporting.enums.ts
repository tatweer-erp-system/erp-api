export enum ReportGranularity {
  DAY = 'day',
  WEEK = 'week',
  MONTH = 'month',
  QUARTER = 'quarter',
  YEAR = 'year',
}

export enum ReportModule {
  SALES = 'sales',
  INVENTORY = 'inventory',
  HR = 'hr',
  FINANCIAL = 'financial',
  CRM = 'crm',
}

export enum ExportFormat {
  PDF = 'pdf',
  CSV = 'csv',
  XLSX = 'xlsx',
}
