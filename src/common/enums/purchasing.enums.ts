export enum PurchaseOrderStatus {
  DRAFT = 'draft',
  SUBMITTED = 'submitted',
  APPROVED = 'approved',
  CONFIRMED = 'confirmed',
  IN_PROGRESS = 'in_progress',
  RECEIVED = 'received',
  CANCELLED = 'cancelled',
}

export enum VendorStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  BLOCKED = 'blocked',
}
