export enum TenantStatus {
  TRIAL = 'trial',
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
  CANCELLED = 'cancelled',
}

export enum TenantNotePriority {
  NORMAL = 'normal',
  URGENT = 'urgent',
}
