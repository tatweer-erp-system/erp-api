export enum SequenceEntity {
  SALES_ORDER = 'sales_order',
  PURCHASE_ORDER = 'purchase_order',
  EMPLOYEE = 'employee',
  LEAD = 'lead',
  PROJECT = 'project',
  ZATCA_INVOICE = 'zatca_invoice',
  POS_ORDER = 'pos_order',
}

export enum ResetCycle {
  NEVER = 'never',
  YEARLY = 'yearly',
  MONTHLY = 'monthly',
}
