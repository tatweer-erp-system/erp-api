export enum TableStatus {
  AVAILABLE = 'available',
  OCCUPIED = 'occupied',
  RESERVED = 'reserved',
  CLEANING = 'cleaning',
}

export enum KitchenTicketStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  READY = 'ready',
  SERVED = 'served',
  CANCELLED = 'cancelled',
}

export enum CourseType {
  APPETIZER = 'appetizer',
  MAIN = 'main',
  DESSERT = 'dessert',
  BEVERAGE = 'beverage',
}
